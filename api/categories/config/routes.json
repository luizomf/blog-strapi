{
  "routes": [
    {
      "method": "GET",
      "path": "/categories",
      "handler": "categories.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/categories/count",
      "handler": "categories.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/categories/:id",
      "handler": "categories.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/categories",
      "handler": "categories.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/categories/:id",
      "handler": "categories.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/categories/:id",
      "handler": "categories.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
